# ============================================================================
# Simple File Manager - Environment Configuration
# ============================================================================
# Copy this file to .env and configure your settings
# Command: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------------------------------------------

# Port number for the web server
PORT=3000

# ----------------------------------------------------------------------------
# UPLOAD DIRECTORY CONFIGURATION
# ----------------------------------------------------------------------------
# IMPORTANT: Choose ONE of the following options and uncomment it
#
# The upload directory is where all uploaded files will be stored.
# You have three options:
#
# Option 1: RELATIVE PATH (Default - Recommended for Security)
# Store files in a directory relative to the application
UPLOAD_DIR=uploads

# Option 2: ABSOLUTE PATH (Requires ALLOW_EXTERNAL_UPLOAD_FOLDER=true)
# Store files anywhere on your system using an absolute path
# Linux/Mac examples:
# UPLOAD_DIR=/var/www/filemanager-uploads
# UPLOAD_DIR=/opt/storage/uploads
# UPLOAD_DIR=/mnt/data/filemanager
#
# Windows examples:
# UPLOAD_DIR=C:/uploads
# UPLOAD_DIR=D:/filemanager/uploads

# Option 3: USER HOME DIRECTORY (Requires ALLOW_EXTERNAL_UPLOAD_FOLDER=true)
# Store files in the current user's home directory using ~ notation
# Linux/Mac examples:
# UPLOAD_DIR=~/filemanager-uploads
# UPLOAD_DIR=~/Documents/uploads
# UPLOAD_DIR=~/storage/files
#
# Note: ~ expands to the home directory of the user running the application
# - For user 'john': ~/uploads becomes /home/john/uploads
# - For user 'ubuntu': ~/uploads becomes /home/ubuntu/uploads
# - On macOS: ~/uploads becomes /Users/username/uploads

# ----------------------------------------------------------------------------
# SECURITY: ALLOW ACCESS OUTSIDE APPLICATION ROOT
# ----------------------------------------------------------------------------
# IMPORTANT SECURITY SETTING!
# 
# Set to 'true' to allow upload directory outside the application folder.
# This is required for Option 2 (absolute paths) or Option 3 (home directory).
#
# Default: false (for security)
#
# ⚠️  SECURITY WARNING:
# Enabling this allows the application to store files outside its own directory.
# Only enable this if:
# 1. You understand the security implications
# 2. The external directory has proper permissions set
# 3. You trust the upload directory location
# 4. You have proper access controls in place
#
# Leave this as 'false' if you're using a relative path (Option 1).
#
ALLOW_EXTERNAL_UPLOAD_FOLDER=false

# To enable external paths (Option 2 or 3), uncomment this line:
# ALLOW_EXTERNAL_UPLOAD_FOLDER=true

# ----------------------------------------------------------------------------
# AUTHENTICATION CONFIGURATION
# ----------------------------------------------------------------------------

# Admin username for login
# Can be a simple username or email address
ADMIN_USERNAME=admin

# Admin password for login
# SECURITY WARNING: Change this before deploying to production!
# Requirements:
# - Minimum 8 characters recommended (12+ for production)
# - Use a mix of letters, numbers, and special characters
# - Don't use common passwords
ADMIN_PASSWORD=admin123

# Session secret for cookie encryption
# SECURITY WARNING: Generate a secure random string for production!
# Generate with: openssl rand -base64 32
# This key encrypts session cookies - keep it secret!
SESSION_SECRET=your-random-secret-key-here

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your settings:
#    nano .env  (or use your preferred editor)
#
# 3. Choose and configure your UPLOAD_DIR option above
#
# 4. If using external directory (Option 2 or 3):
#    - Set ALLOW_EXTERNAL_UPLOAD_FOLDER=true
#    - Ensure proper permissions:
#      sudo mkdir -p /your/chosen/path
#      sudo chown -R $USER:$USER /your/chosen/path
#      sudo chmod 755 /your/chosen/path
#
# 5. Change ADMIN_PASSWORD to a strong password
#
# 6. Generate a secure SESSION_SECRET:
#    openssl rand -base64 32
#
# 7. Start the application:
#    npm start
#
# ============================================================================
# EXAMPLES
# ============================================================================
#
# Example 1: Default (secure, no external access needed)
# UPLOAD_DIR=uploads
# ALLOW_EXTERNAL_UPLOAD_FOLDER=false
#
# Example 2: Store in user home directory
# UPLOAD_DIR=~/filemanager-uploads
# ALLOW_EXTERNAL_UPLOAD_FOLDER=true
#
# Example 3: Store in /opt directory
# UPLOAD_DIR=/opt/filemanager/uploads
# ALLOW_EXTERNAL_UPLOAD_FOLDER=true
#
# Example 4: Store on separate partition
# UPLOAD_DIR=/mnt/storage/uploads
# ALLOW_EXTERNAL_UPLOAD_FOLDER=true
#
# ============================================================================
